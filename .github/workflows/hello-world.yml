name: Hello GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Вивести повідомлення
        run: echo "Hello, GitHub Actions!"
